{
  "name": "@irudd-le/mailbox",
  "version": "0.1.0",
  "description": "Reserved boundary for the durable mailbox service (#34)",
  "private": true,
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm build && node --test build/*.test.js",
    "start": "node build/server.js"
  },
  "dependencies": {
    "@irudd-le/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
