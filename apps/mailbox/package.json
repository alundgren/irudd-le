{
  "name": "@irudd-le/mailbox",
  "version": "0.1.0",
  "description": "Reserved boundary for the durable mailbox service (#34)",
  "private": true,
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm --filter @irudd-le/protocol build && corepack pnpm build && node --test test/**/*.test.mjs"
  },
  "dependencies": {
    "@irudd-le/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
