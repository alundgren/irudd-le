<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for local, ad-hoc-signed macOS builds (identity "-").

  Hardened runtime + ad-hoc signing refuses to load Electron's own frameworks
  without disable-library-validation, and the app exits instantly with no
  explanation. The JIT entitlements are what V8 needs.

  For a real signed release, use a Developer ID identity instead of "-";
  these stay valid either way.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
  </dict>
</plist>
